name: Build and Deploy to Docker Hub

on:
  push:
    branches:
      - master
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
    - name: Check Out Repo
      uses: actions/checkout@v6

    - name: Log in to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Build and Push Docker image
      uses: docker/build-push-action@v6
      with:
        context: .
        file: docker/Dockerfile.user-app
        push: true
        tags: tahirkhan01/okane:latest
    
    - name: Verify Pushed Image
      run: docker pull tahirkhan01/okane:latest

    # - name: Deploy to EC2
    #   uses: appleboy/ssh-action@master
    #   with:
    #     host: ${{ secrets.SSH_HOST }}
    #     username: ${{ secrets.SSH_USERNAME }}
    #     key: ${{ secrets.SSH_KEY }}
    #     script: |
    #       sudo docker pull tahirkhan01/okane:latest
    #       sudo docker stop okane || true
    #       sudo docker rm okane || true
    #       sudo docker run -d --name okane -p 3005:3000 tahirkhan01/okane:latest
